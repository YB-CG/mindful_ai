# Gemini API Key
NEXT_PUBLIC_GEMINI_API_KEY=

# Force using server API proxy for local dev (avoids browser CORS/streaming issues)
NEXT_PUBLIC_GEMINI_USE_SERVER=true

# Explicitly set the model to the one verified by curl
NEXT_PUBLIC_GEMINI_MODEL=gemini-2.5-flash